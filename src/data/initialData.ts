import { Article, SchoolEvent, PhotoSubmission, ArticleComment } from '../types/newspaper';

export const DEFAULT_TEACHER_KEY = 'HERDAR2025';

// Blank initial datasets as requested by user - ready for real articles, events, and photos
export const INITIAL_ARTICLES: Article[] = [];

export const INITIAL_EVENTS: SchoolEvent[] = [];

export const INITIAL_PHOTOS: PhotoSubmission[] = [];

export const INITIAL_COMMENTS: ArticleComment[] = [];
